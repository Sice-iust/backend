from django.contrib import admin
from django.urls import path
from .views import *

urlpatterns = [
    # path('user/reserve/',ReservationView.as_view()),
    # path('nanzi/admin/reserve/',AdminReservationView.as_view()),
    # path('user/create/order/reserve/',OrderReservesView.as_view()),
]
